function [xmin, ymin] = bruteforce(func, start, stop, precision)

    n = ceil((stop - start)/precision);
    x = linspace(start, stop, n);
    y = func(x);
    ymin, ind = min(x);
    xmin = x(ind);
end

f = @(x) x^4 + x^2 + x^1 + 1;
[ab, gh] = bruteforce(f, -1, 0)